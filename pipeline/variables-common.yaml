variables:
  locationEng: 'UK South' # add coalese
  location: 'uksouth' # add coalese
  product: 'hmi'
  builtFrom: 'https://github.com/hmcts/fh-hmi-api-gateway'

  # Need to confirm on the format for the above array

  # Some of the above variables don't seem to be required for creating an API Management instance e.g. sku, publisher, offer, etc.
  # Need to clarify this with the devops team and remove if not required.